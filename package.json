{
  "name": "fage-hardened-v2",
  "version": "1.0.0",
  "description": "FAGE root package",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "start.bat"
  }
}
