# FAGE Release Manifest

This document records the cryptographic hashes, versions, and configuration boundaries for the FAGE release candidate. Every hash below was generated by running `sha256sum` directly against the file in this exact archive — regenerate and compare before trusting a copy.

## Component Versions

- **Git Commit Hash**: not applicable — this archive was not exported via `git archive`/clean checkout, so no verifiable commit hash exists for it. Do not cite a commit hash for this build.
- **Backend Version**: `2.0.0-rc.1`
- **Frontend Build Hash (index.html)**: `cb0d3a2ef997b025e920a196c218cb92ffc8dea02e7afbdae0470018c6ffb034`
- **Model Artifact (xgboost_classifier.pkl) SHA-256**: `10aaa2015fff0a7407040e10ceae9311741b9bbebcb589c653e061a7155b2181`

## ML Pipeline Configuration (FROZEN)

- **Feature Count**: `353`
- **Global Decision Threshold**: `0.60`
- **Leakage Blacklist Version**: `v1.2`
- **Database Seed Version**: `finale_demo_v1`

## Security Boundaries

- ✅ No secrets committed to this manifest's tracked scope (NVIDIA API key loaded from environment, empty by default)
- ✅ JWT secret is fail-fast, not self-rotating: the app refuses to start in production if `FAGE_JWT_SECRET` is unset or left at the insecure development default — it does not automatically rotate secrets
- ✅ Path traversal prevented (SPA catch-all locked to static root)
- ✅ Roles enforced (Analyst/Auditor/Admin RBAC strict matching)
- ✅ Deterministic LLM fallback enabled

## Known Exclusions From This Archive

- `backend/.env`, `DataSet.csv`, and `Description.xlsx` must never be included in any copy of this archive shared outside the team — they are gitignored for a reason (secrets and organizer dataset respectively) and their presence here reflects a raw folder export, not a clean release build.
